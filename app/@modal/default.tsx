// The @modal slot shows nothing unless an intercepted route is active.
// Also rendered after a full page load, when there's no modal state to restore.
export default function ModalDefault() {
  return null;
}
